---
"@gridkitjs/theme-tailwind": patch
---

The last row's cells no longer draw a bottom border under `borders-horizontal` or `borders-all` — previously it rendered as a stray extra edge below the table.
