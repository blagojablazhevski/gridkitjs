---
title: "Overview"
description: "Framework-agnostic grid logic: column inference, ordering, resizing, and selection."
---

@gridkitjs/core is plain TypeScript — so it's testable without a DOM and reusable from other framework adapters. Today it covers column inference, ordering, resizing, and selection; sorting, filtering, and paging are not implemented yet.

## Install

```ts
npm install @gridkitjs/core
```

See [column definitions](/docs/core/column-definitions) for column inference, [resizing](/docs/core/resizing) and [ordering](/docs/core/ordering) for the logic underneath resizableColumns and reorderableColumns, or [selection](/docs/core/selection) for the primitives underneath selectable.
